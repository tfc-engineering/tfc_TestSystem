basic_python:
  executable: "python"
  args: "-c 'print(\"Hello world from python\")'"
  checks: [{type: ExitCodeCheck}]
basic_python1:
  executable: "python"
  args: "-c 'print(\"Hello world from python\")'"
  requirements: ["inparams", "cli1"]
  checks: [{type: ExitCodeCheck}]
basic_python2:
  executable: "python"
  args: "-c 'print(\"Hello world from python\")'"
  requirements: ["utesting", "cconv", "cli1"]
  checks: [{type: ExitCodeCheck}]
skip_test:
  requirements: ["utesting", "cconv", "inparams1", "cli1", "test1"]
  args: ""
  skip: "I just want to skip"
  checks: [{type: ExitCodeCheck}]
